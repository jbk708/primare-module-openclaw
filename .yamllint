# Yamllint config for GitHub Actions workflows and hook files.
# Allow longer lines since GitHub Actions syntax requires it.
rules:
  line-length:
    max: 120
    allow-non-breakable-words: true
  truthy:
    allowed-values: ["true", "false", "on", "off"]
  document-start:
    present: false
