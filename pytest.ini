[pytest]
testpaths = tests
pythonpath = .
addopts = -v --cov=src --cov-report=term-missing --cov-fail-under=80
python_files = test_*.py
markers = integration: integration tests that require a running service
