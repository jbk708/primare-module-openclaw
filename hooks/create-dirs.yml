# create-dirs.yml — create config and workspace directories on the head node.
#
# UID 1000 matches the node user inside the OpenClaw container so the process
# can read/write its own config and workspace without running as root.
- name: "openclaw | create-dirs — config directory (UID 1000)"
  ansible.builtin.file:
    path: "{{ openclaw_config_dir }}"
    state: directory
    owner: "1000"
    group: "1000"
    mode: "0750"

- name: "openclaw | create-dirs — workspace directory (UID 1000)"
  ansible.builtin.file:
    path: "{{ openclaw_workspace_dir }}"
    state: directory
    owner: "1000"
    group: "1000"
    mode: "0750"
